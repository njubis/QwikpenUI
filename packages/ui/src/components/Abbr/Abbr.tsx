import { component$ } from '@builder.io/qwik';

export interface AbbrProps {

}

export const Abbr = component$<AbbrProps>((props) => {
  return (
    <div>
      Abbr component works!
    </div>
  );
});
