import { component$ } from '@builder.io/qwik';

export interface ActionBarProps {

}

export const ActionBar = component$<ActionBarProps>((props) => {
  return (
    <div>
      ActionBar component works!
    </div>
  );
});
