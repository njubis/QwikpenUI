import { component$ } from '@builder.io/qwik';

export interface ActionButtonProps {

}

export const ActionButton = component$<ActionButtonProps>((props) => {
  return (
    <div>
      ActionButton component works!
    </div>
  );
});
