import { component$ } from '@builder.io/qwik';

export interface ActionGroupProps {

}

export const ActionGroup = component$<ActionGroupProps>((props) => {
  return (
    <div>
      ActionGroup component works!
    </div>
  );
});
