import { component$ } from '@builder.io/qwik';

export interface ActivityItemProps {

}

export const ActivityItem = component$<ActivityItemProps>((props) => {
  return (
    <div>
      ActivityItem component works!
    </div>
  );
});
