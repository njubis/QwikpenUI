import { component$ } from '@builder.io/qwik';

export interface ActivityTimelineProps {

}

export const ActivityTimeline = component$<ActivityTimelineProps>((props) => {
  return (
    <div>
      ActivityTimeline component works!
    </div>
  );
});
