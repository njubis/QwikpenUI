import { component$ } from '@builder.io/qwik';

export interface AdvertisementProps {

}

export const Advertisement = component$<AdvertisementProps>((props) => {
  return (
    <div>
      Advertisement component works!
    </div>
  );
});
