import { component$ } from '@builder.io/qwik';

export interface AlertAndMessageDialogsProps {

}

export const AlertAndMessageDialogs = component$<AlertAndMessageDialogsProps>((props) => {
  return (
    <div>
      AlertAndMessageDialogs component works!
    </div>
  );
});
