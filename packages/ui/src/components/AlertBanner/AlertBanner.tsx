import { component$ } from '@builder.io/qwik';

export interface AlertBannerProps {

}

export const AlertBanner = component$<AlertBannerProps>((props) => {
  return (
    <div>
      AlertBanner component works!
    </div>
  );
});
