import { component$ } from '@builder.io/qwik';

export interface AlertDialogProps {

}

export const AlertDialog = component$<AlertDialogProps>((props) => {
  return (
    <div>
      AlertDialog component works!
    </div>
  );
});
