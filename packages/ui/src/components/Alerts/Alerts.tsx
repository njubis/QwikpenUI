import { component$ } from '@builder.io/qwik';

export interface AlertsProps {

}

export const Alerts = component$<AlertsProps>((props) => {
  return (
    <div>
      Alerts component works!
    </div>
  );
});
