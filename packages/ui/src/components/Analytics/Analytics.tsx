import { component$ } from '@builder.io/qwik';

export interface AnalyticsProps {

}

export const Analytics = component$<AnalyticsProps>((props) => {
  return (
    <div>
      Analytics component works!
    </div>
  );
});
