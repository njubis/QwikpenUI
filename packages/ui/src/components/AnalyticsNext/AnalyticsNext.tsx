import { component$ } from '@builder.io/qwik';

export interface AnalyticsNextProps {

}

export const AnalyticsNext = component$<AnalyticsNextProps>((props) => {
  return (
    <div>
      AnalyticsNext component works!
    </div>
  );
});
