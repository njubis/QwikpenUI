import { component$ } from '@builder.io/qwik';

export interface AnchorProps {

}

export const Anchor = component$<AnchorProps>((props) => {
  return (
    <div>
      Anchor component works!
    </div>
  );
});
