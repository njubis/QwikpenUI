import { component$ } from '@builder.io/qwik';

export interface AttachmentProps {

}

export const Attachment = component$<AttachmentProps>((props) => {
  return (
    <div>
      Attachment component works!
    </div>
  );
});
