import { component$ } from '@builder.io/qwik';

export interface AudioProps {

}

export const Audio = component$<AudioProps>((props) => {
  return (
    <div>
      Audio component works!
    </div>
  );
});
