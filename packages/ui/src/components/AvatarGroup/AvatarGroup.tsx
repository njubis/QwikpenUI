import { component$ } from '@builder.io/qwik';

export interface AvatarGroupProps {

}

export const AvatarGroup = component$<AvatarGroupProps>((props) => {
  return (
    <div>
      AvatarGroup component works!
    </div>
  );
});
