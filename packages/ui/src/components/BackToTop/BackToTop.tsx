import { component$ } from '@builder.io/qwik';

export interface BackToTopProps {

}

export const BackToTop = component$<BackToTopProps>((props) => {
  return (
    <div>
      BackToTop component works!
    </div>
  );
});
