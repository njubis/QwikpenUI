import { component$ } from '@builder.io/qwik';

export interface BadgesProps {

}

export const Badges = component$<BadgesProps>((props) => {
  return (
    <div>
      Badges component works!
    </div>
  );
});
