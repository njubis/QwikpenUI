import { component$ } from '@builder.io/qwik';

export interface BasicListProps {

}

export const BasicList = component$<BasicListProps>((props) => {
  return (
    <div>
      BasicList component works!
    </div>
  );
});
