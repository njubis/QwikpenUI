import { component$ } from '@builder.io/qwik';

export interface BottomNavigationProps {

}

export const BottomNavigation = component$<BottomNavigationProps>((props) => {
  return (
    <div>
      BottomNavigation component works!
    </div>
  );
});
