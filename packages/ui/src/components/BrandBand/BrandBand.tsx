import { component$ } from '@builder.io/qwik';

export interface BrandBandProps {

}

export const BrandBand = component$<BrandBandProps>((props) => {
  return (
    <div>
      BrandBand component works!
    </div>
  );
});
