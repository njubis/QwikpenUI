import { component$ } from '@builder.io/qwik';

export interface BuilderHeaderProps {

}

export const BuilderHeader = component$<BuilderHeaderProps>((props) => {
  return (
    <div>
      BuilderHeader component works!
    </div>
  );
});
