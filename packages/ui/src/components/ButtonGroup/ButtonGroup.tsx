import { component$ } from '@builder.io/qwik';

export interface ButtonGroupProps {

}

export const ButtonGroup = component$<ButtonGroupProps>((props) => {
  return (
    <div>
      ButtonGroup component works!
    </div>
  );
});
