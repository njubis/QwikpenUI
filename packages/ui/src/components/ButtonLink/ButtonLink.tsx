import { component$ } from '@builder.io/qwik';

export interface ButtonLinkProps {

}

export const ButtonLink = component$<ButtonLinkProps>((props) => {
  return (
    <div>
      ButtonLink component works!
    </div>
  );
});
