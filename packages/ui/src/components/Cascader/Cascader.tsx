import { component$ } from '@builder.io/qwik';

export interface CascaderProps {

}

export const Cascader = component$<CascaderProps>((props) => {
  return (
    <div>
      Cascader component works!
    </div>
  );
});
