import { component$ } from '@builder.io/qwik';

export interface CheckboxGroupProps {

}

export const CheckboxGroup = component$<CheckboxGroupProps>((props) => {
  return (
    <div>
      CheckboxGroup component works!
    </div>
  );
});
