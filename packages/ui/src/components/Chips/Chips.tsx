import { component$ } from '@builder.io/qwik';

export interface ChipsProps {

}

export const Chips = component$<ChipsProps>((props) => {
  return (
    <div>
      Chips component works!
    </div>
  );
});
