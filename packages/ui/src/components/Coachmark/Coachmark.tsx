import { component$ } from '@builder.io/qwik';

export interface CoachmarkProps {

}

export const Coachmark = component$<CoachmarkProps>((props) => {
  return (
    <div>
      Coachmark component works!
    </div>
  );
});
