import { component$ } from '@builder.io/qwik';

export interface CodeProps {

}

export const Code = component$<CodeProps>((props) => {
  return (
    <div>
      Code component works!
    </div>
  );
});
