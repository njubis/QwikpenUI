import { component$ } from '@builder.io/qwik';

export interface CodePickerProps {

}

export const CodePicker = component$<CodePickerProps>((props) => {
  return (
    <div>
      CodePicker component works!
    </div>
  );
});
