import { component$ } from '@builder.io/qwik';

export interface CodeSnippetProps {

}

export const CodeSnippet = component$<CodeSnippetProps>((props) => {
  return (
    <div>
      CodeSnippet component works!
    </div>
  );
});
