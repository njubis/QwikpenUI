import { component$ } from '@builder.io/qwik';

export interface ColorAreaProps {

}

export const ColorArea = component$<ColorAreaProps>((props) => {
  return (
    <div>
      ColorArea component works!
    </div>
  );
});
