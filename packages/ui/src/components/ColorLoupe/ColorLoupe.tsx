import { component$ } from '@builder.io/qwik';

export interface ColorLoupeProps {

}

export const ColorLoupe = component$<ColorLoupeProps>((props) => {
  return (
    <div>
      ColorLoupe component works!
    </div>
  );
});
