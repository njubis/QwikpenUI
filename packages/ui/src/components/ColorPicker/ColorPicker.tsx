import { component$ } from '@builder.io/qwik';

export interface ColorPickerProps {

}

export const ColorPicker = component$<ColorPickerProps>((props) => {
  return (
    <div>
      ColorPicker component works!
    </div>
  );
});
