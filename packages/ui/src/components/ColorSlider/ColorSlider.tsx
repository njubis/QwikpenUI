import { component$ } from '@builder.io/qwik';

export interface ColorSliderProps {

}

export const ColorSlider = component$<ColorSliderProps>((props) => {
  return (
    <div>
      ColorSlider component works!
    </div>
  );
});
