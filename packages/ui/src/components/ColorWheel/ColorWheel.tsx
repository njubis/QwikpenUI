import { component$ } from '@builder.io/qwik';

export interface ColorWheelProps {

}

export const ColorWheel = component$<ColorWheelProps>((props) => {
  return (
    <div>
      ColorWheel component works!
    </div>
  );
});
