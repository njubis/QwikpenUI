import { component$ } from '@builder.io/qwik';

export interface ComboBoxProps {

}

export const ComboBox = component$<ComboBoxProps>((props) => {
  return (
    <div>
      ComboBox component works!
    </div>
  );
});
