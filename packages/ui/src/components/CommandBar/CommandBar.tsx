import { component$ } from '@builder.io/qwik';

export interface CommandBarProps {

}

export const CommandBar = component$<CommandBarProps>((props) => {
  return (
    <div>
      CommandBar component works!
    </div>
  );
});
