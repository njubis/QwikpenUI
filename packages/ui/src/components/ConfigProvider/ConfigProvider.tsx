import { component$ } from '@builder.io/qwik';

export interface ConfigProviderProps {

}

export const ConfigProvider = component$<ConfigProviderProps>((props) => {
  return (
    <div>
      ConfigProvider component works!
    </div>
  );
});
