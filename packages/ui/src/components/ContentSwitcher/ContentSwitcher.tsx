import { component$ } from '@builder.io/qwik';

export interface ContentSwitcherProps {

}

export const ContentSwitcher = component$<ContentSwitcherProps>((props) => {
  return (
    <div>
      ContentSwitcher component works!
    </div>
  );
});
