import { component$ } from '@builder.io/qwik';

export interface ContextualHelpProps {

}

export const ContextualHelp = component$<ContextualHelpProps>((props) => {
  return (
    <div>
      ContextualHelp component works!
    </div>
  );
});
