import { component$ } from '@builder.io/qwik';

export interface ContextualMenuProps {

}

export const ContextualMenu = component$<ContextualMenuProps>((props) => {
  return (
    <div>
      ContextualMenu component works!
    </div>
  );
});
