import { component$ } from '@builder.io/qwik';

export interface DataGridProps {

}

export const DataGrid = component$<DataGridProps>((props) => {
  return (
    <div>
      DataGrid component works!
    </div>
  );
});
