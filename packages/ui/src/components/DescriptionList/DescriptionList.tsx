import { component$ } from '@builder.io/qwik';

export interface DescriptionListProps {

}

export const DescriptionList = component$<DescriptionListProps>((props) => {
  return (
    <div>
      DescriptionList component works!
    </div>
  );
});
