import { component$ } from '@builder.io/qwik';

export interface DimmerProps {

}

export const Dimmer = component$<DimmerProps>((props) => {
  return (
    <div>
      Dimmer component works!
    </div>
  );
});
