import { component$ } from '@builder.io/qwik';

export interface DisclosureProps {

}

export const Disclosure = component$<DisclosureProps>((props) => {
  return (
    <div>
      Disclosure component works!
    </div>
  );
});
