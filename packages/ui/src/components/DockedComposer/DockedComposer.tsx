import { component$ } from '@builder.io/qwik';

export interface DockedComposerProps {

}

export const DockedComposer = component$<DockedComposerProps>((props) => {
  return (
    <div>
      DockedComposer component works!
    </div>
  );
});
