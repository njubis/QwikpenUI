import { component$ } from '@builder.io/qwik';

export interface DockedFormFooterProps {

}

export const DockedFormFooter = component$<DockedFormFooterProps>((props) => {
  return (
    <div>
      DockedFormFooter component works!
    </div>
  );
});
