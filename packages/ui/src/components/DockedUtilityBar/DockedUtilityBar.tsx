import { component$ } from '@builder.io/qwik';

export interface DockedUtilityBarProps {

}

export const DockedUtilityBar = component$<DockedUtilityBarProps>((props) => {
  return (
    <div>
      DockedUtilityBar component works!
    </div>
  );
});
