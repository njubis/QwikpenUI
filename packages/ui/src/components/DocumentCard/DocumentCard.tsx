import { component$ } from '@builder.io/qwik';

export interface DocumentCardProps {

}

export const DocumentCard = component$<DocumentCardProps>((props) => {
  return (
    <div>
      DocumentCard component works!
    </div>
  );
});
