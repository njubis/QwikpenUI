import { component$ } from '@builder.io/qwik';

export interface DocumentationProps {

}

export const Documentation = component$<DocumentationProps>((props) => {
  return (
    <div>
      Documentation component works!
    </div>
  );
});
