import { component$ } from '@builder.io/qwik';

export interface DropZoneProps {

}

export const DropZone = component$<DropZoneProps>((props) => {
  return (
    <div>
      DropZone component works!
    </div>
  );
});
