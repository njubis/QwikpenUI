import { component$ } from '@builder.io/qwik';

export interface DropdownProps {

}

export const Dropdown = component$<DropdownProps>((props) => {
  return (
    <div>
      Dropdown component works!
    </div>
  );
});
