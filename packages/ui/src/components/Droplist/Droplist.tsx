import { component$ } from '@builder.io/qwik';

export interface DroplistProps {

}

export const Droplist = component$<DroplistProps>((props) => {
  return (
    <div>
      Droplist component works!
    </div>
  );
});
