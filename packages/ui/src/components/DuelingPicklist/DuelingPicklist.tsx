import { component$ } from '@builder.io/qwik';

export interface DuelingPicklistProps {

}

export const DuelingPicklist = component$<DuelingPicklistProps>((props) => {
  return (
    <div>
      DuelingPicklist component works!
    </div>
  );
});
