import { component$ } from '@builder.io/qwik';

export interface DynamicIconsProps {

}

export const DynamicIcons = component$<DynamicIconsProps>((props) => {
  return (
    <div>
      DynamicIcons component works!
    </div>
  );
});
