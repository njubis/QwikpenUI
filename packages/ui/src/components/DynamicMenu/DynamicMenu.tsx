import { component$ } from '@builder.io/qwik';

export interface DynamicMenuProps {

}

export const DynamicMenu = component$<DynamicMenuProps>((props) => {
  return (
    <div>
      DynamicMenu component works!
    </div>
  );
});
