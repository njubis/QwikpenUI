import { component$ } from '@builder.io/qwik';

export interface ElevationProps {

}

export const Elevation = component$<ElevationProps>((props) => {
  return (
    <div>
      Elevation component works!
    </div>
  );
});
