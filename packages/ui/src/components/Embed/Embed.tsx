import { component$ } from '@builder.io/qwik';

export interface EmbedProps {

}

export const Embed = component$<EmbedProps>((props) => {
  return (
    <div>
      Embed component works!
    </div>
  );
});
