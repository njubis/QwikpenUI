import { component$ } from '@builder.io/qwik';

export interface ExpandableSectionProps {

}

export const ExpandableSection = component$<ExpandableSectionProps>((props) => {
  return (
    <div>
      ExpandableSection component works!
    </div>
  );
});
