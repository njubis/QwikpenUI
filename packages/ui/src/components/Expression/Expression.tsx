import { component$ } from '@builder.io/qwik';

export interface ExpressionProps {

}

export const Expression = component$<ExpressionProps>((props) => {
  return (
    <div>
      Expression component works!
    </div>
  );
});
