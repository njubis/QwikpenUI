import { component$ } from '@builder.io/qwik';

export interface FacepileProps {

}

export const Facepile = component$<FacepileProps>((props) => {
  return (
    <div>
      Facepile component works!
    </div>
  );
});
