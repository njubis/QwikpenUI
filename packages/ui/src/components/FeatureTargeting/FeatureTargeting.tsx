import { component$ } from '@builder.io/qwik';

export interface FeatureTargetingProps {

}

export const FeatureTargeting = component$<FeatureTargetingProps>((props) => {
  return (
    <div>
      FeatureTargeting component works!
    </div>
  );
});
