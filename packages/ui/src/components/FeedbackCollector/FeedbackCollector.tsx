import { component$ } from '@builder.io/qwik';

export interface FeedbackCollectorProps {

}

export const FeedbackCollector = component$<FeedbackCollectorProps>((props) => {
  return (
    <div>
      FeedbackCollector component works!
    </div>
  );
});
