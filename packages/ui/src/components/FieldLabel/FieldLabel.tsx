import { component$ } from '@builder.io/qwik';

export interface FieldLabelProps {

}

export const FieldLabel = component$<FieldLabelProps>((props) => {
  return (
    <div>
      FieldLabel component works!
    </div>
  );
});
