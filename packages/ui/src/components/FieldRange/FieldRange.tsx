import { component$ } from '@builder.io/qwik';

export interface FieldRangeProps {

}

export const FieldRange = component$<FieldRangeProps>((props) => {
  return (
    <div>
      FieldRange component works!
    </div>
  );
});
