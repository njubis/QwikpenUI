import { component$ } from '@builder.io/qwik';

export interface FieldTextProps {

}

export const FieldText = component$<FieldTextProps>((props) => {
  return (
    <div>
      FieldText component works!
    </div>
  );
});
