import { component$ } from '@builder.io/qwik';

export interface FieldTextAreaProps {

}

export const FieldTextArea = component$<FieldTextAreaProps>((props) => {
  return (
    <div>
      FieldTextArea component works!
    </div>
  );
});
