import { component$ } from '@builder.io/qwik';

export interface FlagProps {

}

export const Flag = component$<FlagProps>((props) => {
  return (
    <div>
      Flag component works!
    </div>
  );
});
