import { component$ } from '@builder.io/qwik';

export interface FlipperProps {

}

export const Flipper = component$<FlipperProps>((props) => {
  return (
    <div>
      Flipper component works!
    </div>
  );
});
