import { component$ } from '@builder.io/qwik';

export interface FloatingActionButtonProps {

}

export const FloatingActionButton = component$<FloatingActionButtonProps>((props) => {
  return (
    <div>
      FloatingActionButton component works!
    </div>
  );
});
