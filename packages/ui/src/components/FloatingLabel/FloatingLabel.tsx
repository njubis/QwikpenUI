import { component$ } from '@builder.io/qwik';

export interface FloatingLabelProps {

}

export const FloatingLabel = component$<FloatingLabelProps>((props) => {
  return (
    <div>
      FloatingLabel component works!
    </div>
  );
});
