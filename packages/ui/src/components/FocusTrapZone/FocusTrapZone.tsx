import { component$ } from '@builder.io/qwik';

export interface FocusTrapZoneProps {

}

export const FocusTrapZone = component$<FocusTrapZoneProps>((props) => {
  return (
    <div>
      FocusTrapZone component works!
    </div>
  );
});
