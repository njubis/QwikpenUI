import { component$ } from '@builder.io/qwik';

export interface FocusZoneProps {

}

export const FocusZone = component$<FocusZoneProps>((props) => {
  return (
    <div>
      FocusZone component works!
    </div>
  );
});
