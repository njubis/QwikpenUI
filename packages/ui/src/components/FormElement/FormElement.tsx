import { component$ } from '@builder.io/qwik';

export interface FormElementProps {

}

export const FormElement = component$<FormElementProps>((props) => {
  return (
    <div>
      FormElement component works!
    </div>
  );
});
