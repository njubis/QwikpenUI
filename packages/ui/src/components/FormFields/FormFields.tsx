import { component$ } from '@builder.io/qwik';

export interface FormFieldsProps {

}

export const FormFields = component$<FormFieldsProps>((props) => {
  return (
    <div>
      FormFields component works!
    </div>
  );
});
