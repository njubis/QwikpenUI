import { component$ } from '@builder.io/qwik';

export interface FormValidationProps {

}

export const FormValidation = component$<FormValidationProps>((props) => {
  return (
    <div>
      FormValidation component works!
    </div>
  );
});
