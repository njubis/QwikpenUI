import { component$ } from '@builder.io/qwik';

export interface FormsProps {

}

export const Forms = component$<FormsProps>((props) => {
  return (
    <div>
      Forms component works!
    </div>
  );
});
