import { component$ } from '@builder.io/qwik';

export interface GlobalNavigationProps {

}

export const GlobalNavigation = component$<GlobalNavigationProps>((props) => {
  return (
    <div>
      GlobalNavigation component works!
    </div>
  );
});
