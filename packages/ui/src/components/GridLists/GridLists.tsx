import { component$ } from '@builder.io/qwik';

export interface GridListsProps {

}

export const GridLists = component$<GridListsProps>((props) => {
  return (
    <div>
      GridLists component works!
    </div>
  );
});
