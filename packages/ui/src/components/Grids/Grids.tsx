import { component$ } from '@builder.io/qwik';

export interface GridsProps {

}

export const Grids = component$<GridsProps>((props) => {
  return (
    <div>
      Grids component works!
    </div>
  );
});
