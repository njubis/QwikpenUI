import { component$ } from '@builder.io/qwik';

export interface GroupedListProps {

}

export const GroupedList = component$<GroupedListProps>((props) => {
  return (
    <div>
      GroupedList component works!
    </div>
  );
});
