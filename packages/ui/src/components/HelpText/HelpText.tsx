import { component$ } from '@builder.io/qwik';

export interface HelpTextProps {

}

export const HelpText = component$<HelpTextProps>((props) => {
  return (
    <div>
      HelpText component works!
    </div>
  );
});
