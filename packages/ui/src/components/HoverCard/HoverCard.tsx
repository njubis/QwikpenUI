import { component$ } from '@builder.io/qwik';

export interface HoverCardProps {

}

export const HoverCard = component$<HoverCardProps>((props) => {
  return (
    <div>
      HoverCard component works!
    </div>
  );
});
