import { component$ } from '@builder.io/qwik';

export interface IllustrationProps {

}

export const Illustration = component$<IllustrationProps>((props) => {
  return (
    <div>
      Illustration component works!
    </div>
  );
});
