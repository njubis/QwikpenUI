import { component$ } from '@builder.io/qwik';

export interface ImageListProps {

}

export const ImageList = component$<ImageListProps>((props) => {
  return (
    <div>
      ImageList component works!
    </div>
  );
});
