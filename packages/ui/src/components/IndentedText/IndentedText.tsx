import { component$ } from '@builder.io/qwik';

export interface IndentedTextProps {

}

export const IndentedText = component$<IndentedTextProps>((props) => {
  return (
    <div>
      IndentedText component works!
    </div>
  );
});
