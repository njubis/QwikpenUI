import { component$ } from '@builder.io/qwik';

export interface InlineEditProps {

}

export const InlineEdit = component$<InlineEditProps>((props) => {
  return (
    <div>
      InlineEdit component works!
    </div>
  );
});
