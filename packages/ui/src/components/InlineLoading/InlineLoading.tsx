import { component$ } from '@builder.io/qwik';

export interface InlineLoadingProps {

}

export const InlineLoading = component$<InlineLoadingProps>((props) => {
  return (
    <div>
      InlineLoading component works!
    </div>
  );
});
