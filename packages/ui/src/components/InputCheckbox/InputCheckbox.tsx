import { component$ } from '@builder.io/qwik';

export interface InputCheckboxProps {

}

export const InputCheckbox = component$<InputCheckboxProps>((props) => {
  return (
    <div>
      InputCheckbox component works!
    </div>
  );
});
