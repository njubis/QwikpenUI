import { component$ } from '@builder.io/qwik';

export interface InputColorProps {

}

export const InputColor = component$<InputColorProps>((props) => {
  return (
    <div>
      InputColor component works!
    </div>
  );
});
