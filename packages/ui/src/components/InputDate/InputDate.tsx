import { component$ } from '@builder.io/qwik';

export interface InputDateProps {

}

export const InputDate = component$<InputDateProps>((props) => {
  return (
    <div>
      InputDate component works!
    </div>
  );
});
