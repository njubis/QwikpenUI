import { component$ } from '@builder.io/qwik';

export interface InputEmailProps {

}

export const InputEmail = component$<InputEmailProps>((props) => {
  return (
    <div>
      InputEmail component works!
    </div>
  );
});
