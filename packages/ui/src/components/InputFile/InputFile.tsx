import { component$ } from '@builder.io/qwik';

export interface InputFileProps {

}

export const InputFile = component$<InputFileProps>((props) => {
  return (
    <div>
      InputFile component works!
    </div>
  );
});
