import { component$ } from '@builder.io/qwik';

export interface InputGroupProps {

}

export const InputGroup = component$<InputGroupProps>((props) => {
  return (
    <div>
      InputGroup component works!
    </div>
  );
});
