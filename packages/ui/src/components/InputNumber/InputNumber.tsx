import { component$ } from '@builder.io/qwik';

export interface InputNumberProps {

}

export const InputNumber = component$<InputNumberProps>((props) => {
  return (
    <div>
      InputNumber component works!
    </div>
  );
});
