import { component$ } from '@builder.io/qwik';

export interface InputPasswordProps {

}

export const InputPassword = component$<InputPasswordProps>((props) => {
  return (
    <div>
      InputPassword component works!
    </div>
  );
});
