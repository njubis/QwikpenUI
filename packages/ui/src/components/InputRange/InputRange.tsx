import { component$ } from '@builder.io/qwik';

export interface InputRangeProps {

}

export const InputRange = component$<InputRangeProps>((props) => {
  return (
    <div>
      InputRange component works!
    </div>
  );
});
