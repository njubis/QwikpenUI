import { component$ } from '@builder.io/qwik';

export interface InputTextProps {

}

export const InputText = component$<InputTextProps>((props) => {
  return (
    <div>
      InputText component works!
    </div>
  );
});
