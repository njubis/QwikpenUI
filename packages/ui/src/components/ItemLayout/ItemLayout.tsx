import { component$ } from '@builder.io/qwik';

export interface ItemLayoutProps {

}

export const ItemLayout = component$<ItemLayoutProps>((props) => {
  return (
    <div>
      ItemLayout component works!
    </div>
  );
});
