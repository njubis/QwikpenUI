import { component$ } from '@builder.io/qwik';

export interface JumbotronProps {

}

export const Jumbotron = component$<JumbotronProps>((props) => {
  return (
    <div>
      Jumbotron component works!
    </div>
  );
});
