import { component$ } from '@builder.io/qwik';

export interface KeytipsProps {

}

export const Keytips = component$<KeytipsProps>((props) => {
  return (
    <div>
      Keytips component works!
    </div>
  );
});
