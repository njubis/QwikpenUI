import { component$ } from '@builder.io/qwik';

export interface LayerManagerProps {

}

export const LayerManager = component$<LayerManagerProps>((props) => {
  return (
    <div>
      LayerManager component works!
    </div>
  );
});
