import { component$ } from '@builder.io/qwik';

export interface LegendProps {

}

export const Legend = component$<LegendProps>((props) => {
  return (
    <div>
      Legend component works!
    </div>
  );
});
