import { component$ } from '@builder.io/qwik';

export interface LineRippleProps {

}

export const LineRipple = component$<LineRippleProps>((props) => {
  return (
    <div>
      LineRipple component works!
    </div>
  );
});
