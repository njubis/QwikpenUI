import { component$ } from '@builder.io/qwik';

export interface LinearProgressProps {

}

export const LinearProgress = component$<LinearProgressProps>((props) => {
  return (
    <div>
      LinearProgress component works!
    </div>
  );
});
