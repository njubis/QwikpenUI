import { component$ } from '@builder.io/qwik';

export interface ListBuilderProps {

}

export const ListBuilder = component$<ListBuilderProps>((props) => {
  return (
    <div>
      ListBuilder component works!
    </div>
  );
});
