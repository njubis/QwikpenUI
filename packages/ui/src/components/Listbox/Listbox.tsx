import { component$ } from '@builder.io/qwik';

export interface ListboxProps {

}

export const Listbox = component$<ListboxProps>((props) => {
  return (
    <div>
      Listbox component works!
    </div>
  );
});
