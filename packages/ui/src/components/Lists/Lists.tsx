import { component$ } from '@builder.io/qwik';

export interface ListsProps {

}

export const Lists = component$<ListsProps>((props) => {
  return (
    <div>
      Lists component works!
    </div>
  );
});
