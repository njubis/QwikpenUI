import { component$ } from '@builder.io/qwik';

export interface LookupsProps {

}

export const Lookups = component$<LookupsProps>((props) => {
  return (
    <div>
      Lookups component works!
    </div>
  );
});
