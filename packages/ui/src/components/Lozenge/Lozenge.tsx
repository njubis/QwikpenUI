import { component$ } from '@builder.io/qwik';

export interface LozengeProps {

}

export const Lozenge = component$<LozengeProps>((props) => {
  return (
    <div>
      Lozenge component works!
    </div>
  );
});
