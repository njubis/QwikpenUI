import { component$ } from '@builder.io/qwik';

export interface MarqueeSelectionProps {

}

export const MarqueeSelection = component$<MarqueeSelectionProps>((props) => {
  return (
    <div>
      MarqueeSelection component works!
    </div>
  );
});
