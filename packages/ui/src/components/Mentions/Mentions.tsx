import { component$ } from '@builder.io/qwik';

export interface MentionsProps {

}

export const Mentions = component$<MentionsProps>((props) => {
  return (
    <div>
      Mentions component works!
    </div>
  );
});
