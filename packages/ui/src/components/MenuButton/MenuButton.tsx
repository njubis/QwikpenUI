import { component$ } from '@builder.io/qwik';

export interface MenuButtonProps {

}

export const MenuButton = component$<MenuButtonProps>((props) => {
  return (
    <div>
      MenuButton component works!
    </div>
  );
});
