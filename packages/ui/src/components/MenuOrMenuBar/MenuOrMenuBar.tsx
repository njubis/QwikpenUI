import { component$ } from '@builder.io/qwik';

export interface MenuOrMenuBarProps {

}

export const MenuOrMenuBar = component$<MenuOrMenuBarProps>((props) => {
  return (
    <div>
      MenuOrMenuBar component works!
    </div>
  );
});
