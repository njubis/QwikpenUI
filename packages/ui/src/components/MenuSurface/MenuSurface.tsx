import { component$ } from '@builder.io/qwik';

export interface MenuSurfaceProps {

}

export const MenuSurface = component$<MenuSurfaceProps>((props) => {
  return (
    <div>
      MenuSurface component works!
    </div>
  );
});
