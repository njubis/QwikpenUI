import { component$ } from '@builder.io/qwik';

export interface MeterProps {

}

export const Meter = component$<MeterProps>((props) => {
  return (
    <div>
      Meter component works!
    </div>
  );
});
