import { component$ } from '@builder.io/qwik';

export interface ModalsProps {

}

export const Modals = component$<ModalsProps>((props) => {
  return (
    <div>
      Modals component works!
    </div>
  );
});
