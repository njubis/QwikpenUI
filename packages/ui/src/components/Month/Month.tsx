import { component$ } from '@builder.io/qwik';

export interface MonthProps {

}

export const Month = component$<MonthProps>((props) => {
  return (
    <div>
      Month component works!
    </div>
  );
});
