import { component$ } from '@builder.io/qwik';

export interface MultiselectProps {

}

export const Multiselect = component$<MultiselectProps>((props) => {
  return (
    <div>
      Multiselect component works!
    </div>
  );
});
