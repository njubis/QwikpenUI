import { component$ } from '@builder.io/qwik';

export interface NavigationNextProps {

}

export const NavigationNext = component$<NavigationNextProps>((props) => {
  return (
    <div>
      NavigationNext component works!
    </div>
  );
});
