import { component$ } from '@builder.io/qwik';

export interface NavsProps {

}

export const Navs = component$<NavsProps>((props) => {
  return (
    <div>
      Navs component works!
    </div>
  );
});
