import { component$ } from '@builder.io/qwik';

export interface NotchedOutlineProps {

}

export const NotchedOutline = component$<NotchedOutlineProps>((props) => {
  return (
    <div>
      NotchedOutline component works!
    </div>
  );
});
