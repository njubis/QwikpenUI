import { component$ } from '@builder.io/qwik';

export interface NotificationsProps {

}

export const Notifications = component$<NotificationsProps>((props) => {
  return (
    <div>
      Notifications component works!
    </div>
  );
});
