import { component$ } from '@builder.io/qwik';

export interface NpsProps {

}

export const Nps = component$<NpsProps>((props) => {
  return (
    <div>
      Nps component works!
    </div>
  );
});
