import { component$ } from '@builder.io/qwik';

export interface OffcanvasProps {

}

export const Offcanvas = component$<OffcanvasProps>((props) => {
  return (
    <div>
      Offcanvas component works!
    </div>
  );
});
