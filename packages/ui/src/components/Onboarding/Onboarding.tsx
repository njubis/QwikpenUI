import { component$ } from '@builder.io/qwik';

export interface OnboardingProps {

}

export const Onboarding = component$<OnboardingProps>((props) => {
  return (
    <div>
      Onboarding component works!
    </div>
  );
});
