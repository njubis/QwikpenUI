import { component$ } from '@builder.io/qwik';

export interface OverflowSetProps {

}

export const OverflowSet = component$<OverflowSetProps>((props) => {
  return (
    <div>
      OverflowSet component works!
    </div>
  );
});
