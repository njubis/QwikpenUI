import { component$ } from '@builder.io/qwik';

export interface OverviewProps {

}

export const Overview = component$<OverviewProps>((props) => {
  return (
    <div>
      Overview component works!
    </div>
  );
});
