import { component$ } from '@builder.io/qwik';

export interface PageHeaderProps {

}

export const PageHeader = component$<PageHeaderProps>((props) => {
  return (
    <div>
      PageHeader component works!
    </div>
  );
});
