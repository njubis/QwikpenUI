import { component$ } from '@builder.io/qwik';

export interface PageHeadersProps {

}

export const PageHeaders = component$<PageHeadersProps>((props) => {
  return (
    <div>
      PageHeaders component works!
    </div>
  );
});
