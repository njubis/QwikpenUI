import { component$ } from '@builder.io/qwik';

export interface PanelsProps {

}

export const Panels = component$<PanelsProps>((props) => {
  return (
    <div>
      Panels component works!
    </div>
  );
});
