import { component$ } from '@builder.io/qwik';

export interface PasswordProps {

}

export const Password = component$<PasswordProps>((props) => {
  return (
    <div>
      Password component works!
    </div>
  );
});
