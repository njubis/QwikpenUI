import { component$ } from '@builder.io/qwik';

export interface PathProps {

}

export const Path = component$<PathProps>((props) => {
  return (
    <div>
      Path component works!
    </div>
  );
});
