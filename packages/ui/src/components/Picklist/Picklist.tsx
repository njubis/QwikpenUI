import { component$ } from '@builder.io/qwik';

export interface PicklistProps {

}

export const Picklist = component$<PicklistProps>((props) => {
  return (
    <div>
      Picklist component works!
    </div>
  );
});
