import { component$ } from '@builder.io/qwik';

export interface PillProps {

}

export const Pill = component$<PillProps>((props) => {
  return (
    <div>
      Pill component works!
    </div>
  );
});
