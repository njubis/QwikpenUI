import { component$ } from '@builder.io/qwik';

export interface PivotProps {

}

export const Pivot = component$<PivotProps>((props) => {
  return (
    <div>
      Pivot component works!
    </div>
  );
});
