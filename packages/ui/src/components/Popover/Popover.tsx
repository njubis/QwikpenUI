import { component$ } from '@builder.io/qwik';

export interface PopoverProps {

}

export const Popover = component$<PopoverProps>((props) => {
  return (
    <div>
      Popover component works!
    </div>
  );
});
