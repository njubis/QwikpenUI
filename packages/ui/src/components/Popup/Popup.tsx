import { component$ } from '@builder.io/qwik';

export interface PopupProps {

}

export const Popup = component$<PopupProps>((props) => {
  return (
    <div>
      Popup component works!
    </div>
  );
});
