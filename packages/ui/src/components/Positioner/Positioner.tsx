import { component$ } from '@builder.io/qwik';

export interface PositionerProps {

}

export const Positioner = component$<PositionerProps>((props) => {
  return (
    <div>
      Positioner component works!
    </div>
  );
});
