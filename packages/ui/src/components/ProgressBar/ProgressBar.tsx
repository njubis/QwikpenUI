import { component$ } from '@builder.io/qwik';

export interface ProgressBarProps {

}

export const ProgressBar = component$<ProgressBarProps>((props) => {
  return (
    <div>
      ProgressBar component works!
    </div>
  );
});
