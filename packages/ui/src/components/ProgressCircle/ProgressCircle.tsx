import { component$ } from '@builder.io/qwik';

export interface ProgressCircleProps {

}

export const ProgressCircle = component$<ProgressCircleProps>((props) => {
  return (
    <div>
      ProgressCircle component works!
    </div>
  );
});
