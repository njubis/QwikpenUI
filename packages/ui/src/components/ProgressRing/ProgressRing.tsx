import { component$ } from '@builder.io/qwik';

export interface ProgressRingProps {

}

export const ProgressRing = component$<ProgressRingProps>((props) => {
  return (
    <div>
      ProgressRing component works!
    </div>
  );
});
