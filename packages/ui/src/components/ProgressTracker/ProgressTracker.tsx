import { component$ } from '@builder.io/qwik';

export interface ProgressTrackerProps {

}

export const ProgressTracker = component$<ProgressTrackerProps>((props) => {
  return (
    <div>
      ProgressTracker component works!
    </div>
  );
});
