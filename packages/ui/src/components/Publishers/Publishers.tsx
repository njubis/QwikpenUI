import { component$ } from '@builder.io/qwik';

export interface PublishersProps {

}

export const Publishers = component$<PublishersProps>((props) => {
  return (
    <div>
      Publishers component works!
    </div>
  );
});
