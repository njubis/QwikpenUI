import { component$ } from '@builder.io/qwik';

export interface QuantitySelectorProps {

}

export const QuantitySelector = component$<QuantitySelectorProps>((props) => {
  return (
    <div>
      QuantitySelector component works!
    </div>
  );
});
