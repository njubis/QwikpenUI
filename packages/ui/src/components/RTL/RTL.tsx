import { component$ } from '@builder.io/qwik';

export interface RTLProps {

}

export const RTL = component$<RTLProps>((props) => {
  return (
    <div>
      RTL component works!
    </div>
  );
});
