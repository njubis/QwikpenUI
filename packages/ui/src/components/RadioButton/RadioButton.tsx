import { component$ } from '@builder.io/qwik';

export interface RadioButtonProps {

}

export const RadioButton = component$<RadioButtonProps>((props) => {
  return (
    <div>
      RadioButton component works!
    </div>
  );
});
