import { component$ } from '@builder.io/qwik';

export interface RadioButtonGroupProps {

}

export const RadioButtonGroup = component$<RadioButtonGroupProps>((props) => {
  return (
    <div>
      RadioButtonGroup component works!
    </div>
  );
});
