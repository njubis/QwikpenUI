import { component$ } from '@builder.io/qwik';

export interface RadioGroupProps {

}

export const RadioGroup = component$<RadioGroupProps>((props) => {
  return (
    <div>
      RadioGroup component works!
    </div>
  );
});
