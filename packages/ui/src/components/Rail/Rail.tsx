import { component$ } from '@builder.io/qwik';

export interface RailProps {

}

export const Rail = component$<RailProps>((props) => {
  return (
    <div>
      Rail component works!
    </div>
  );
});
