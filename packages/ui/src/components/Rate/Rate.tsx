import { component$ } from '@builder.io/qwik';

export interface RateProps {

}

export const Rate = component$<RateProps>((props) => {
  return (
    <div>
      Rate component works!
    </div>
  );
});
