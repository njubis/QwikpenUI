import { component$ } from '@builder.io/qwik';

export interface ReactionProps {

}

export const Reaction = component$<ReactionProps>((props) => {
  return (
    <div>
      Reaction component works!
    </div>
  );
});
