import { component$ } from '@builder.io/qwik';

export interface ResetProps {

}

export const Reset = component$<ResetProps>((props) => {
  return (
    <div>
      Reset component works!
    </div>
  );
});
