import { component$ } from '@builder.io/qwik';

export interface ResizeGroupProps {

}

export const ResizeGroup = component$<ResizeGroupProps>((props) => {
  return (
    <div>
      ResizeGroup component works!
    </div>
  );
});
