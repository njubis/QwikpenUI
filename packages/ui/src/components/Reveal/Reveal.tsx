import { component$ } from '@builder.io/qwik';

export interface RevealProps {

}

export const Reveal = component$<RevealProps>((props) => {
  return (
    <div>
      Reveal component works!
    </div>
  );
});
