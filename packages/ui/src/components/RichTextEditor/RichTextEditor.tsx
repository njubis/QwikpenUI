import { component$ } from '@builder.io/qwik';

export interface RichTextEditorProps {

}

export const RichTextEditor = component$<RichTextEditorProps>((props) => {
  return (
    <div>
      RichTextEditor component works!
    </div>
  );
});
