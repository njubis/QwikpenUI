import { component$ } from '@builder.io/qwik';

export interface RightSidePanelProps {

}

export const RightSidePanel = component$<RightSidePanelProps>((props) => {
  return (
    <div>
      RightSidePanel component works!
    </div>
  );
});
