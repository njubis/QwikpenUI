import { component$ } from '@builder.io/qwik';

export interface RipplesProps {

}

export const Ripples = component$<RipplesProps>((props) => {
  return (
    <div>
      Ripples component works!
    </div>
  );
});
