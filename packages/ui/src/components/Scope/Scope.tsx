import { component$ } from '@builder.io/qwik';

export interface ScopeProps {

}

export const Scope = component$<ScopeProps>((props) => {
  return (
    <div>
      Scope component works!
    </div>
  );
});
