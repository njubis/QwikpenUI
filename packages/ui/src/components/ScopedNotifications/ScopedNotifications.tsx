import { component$ } from '@builder.io/qwik';

export interface ScopedNotificationsProps {

}

export const ScopedNotifications = component$<ScopedNotificationsProps>((props) => {
  return (
    <div>
      ScopedNotifications component works!
    </div>
  );
});
