import { component$ } from '@builder.io/qwik';

export interface ScopedTabsProps {

}

export const ScopedTabs = component$<ScopedTabsProps>((props) => {
  return (
    <div>
      ScopedTabs component works!
    </div>
  );
});
