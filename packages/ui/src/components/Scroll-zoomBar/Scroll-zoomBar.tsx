import { component$ } from '@builder.io/qwik';

export interface ScrollZoomBarProps {

}

export const ScrollZoomBar = component$<ScrollZoomBarProps>((props) => {
  return (
    <div>
      ScrollZoomBar component works!
    </div>
  );
});
