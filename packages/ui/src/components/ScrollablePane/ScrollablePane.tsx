import { component$ } from '@builder.io/qwik';

export interface ScrollablePaneProps {

}

export const ScrollablePane = component$<ScrollablePaneProps>((props) => {
  return (
    <div>
      ScrollablePane component works!
    </div>
  );
});
