import { component$ } from '@builder.io/qwik';

export interface ScrollspyProps {

}

export const Scrollspy = component$<ScrollspyProps>((props) => {
  return (
    <div>
      Scrollspy component works!
    </div>
  );
});
