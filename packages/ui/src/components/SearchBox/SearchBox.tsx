import { component$ } from '@builder.io/qwik';

export interface SearchBoxProps {

}

export const SearchBox = component$<SearchBoxProps>((props) => {
  return (
    <div>
      SearchBox component works!
    </div>
  );
});
