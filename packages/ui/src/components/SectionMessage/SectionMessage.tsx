import { component$ } from '@builder.io/qwik';

export interface SectionMessageProps {

}

export const SectionMessage = component$<SectionMessageProps>((props) => {
  return (
    <div>
      SectionMessage component works!
    </div>
  );
});
