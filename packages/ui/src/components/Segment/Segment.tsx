import { component$ } from '@builder.io/qwik';

export interface SegmentProps {

}

export const Segment = component$<SegmentProps>((props) => {
  return (
    <div>
      Segment component works!
    </div>
  );
});
