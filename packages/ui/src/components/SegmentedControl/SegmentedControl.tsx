import { component$ } from '@builder.io/qwik';

export interface SegmentedControlProps {

}

export const SegmentedControl = component$<SegmentedControlProps>((props) => {
  return (
    <div>
      SegmentedControl component works!
    </div>
  );
});
