import { component$ } from '@builder.io/qwik';

export interface SelectHelperTextProps {

}

export const SelectHelperText = component$<SelectHelperTextProps>((props) => {
  return (
    <div>
      SelectHelperText component works!
    </div>
  );
});
