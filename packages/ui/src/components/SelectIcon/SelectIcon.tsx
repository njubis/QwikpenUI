import { component$ } from '@builder.io/qwik';

export interface SelectIconProps {

}

export const SelectIcon = component$<SelectIconProps>((props) => {
  return (
    <div>
      SelectIcon component works!
    </div>
  );
});
