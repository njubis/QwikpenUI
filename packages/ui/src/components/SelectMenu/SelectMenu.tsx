import { component$ } from '@builder.io/qwik';

export interface SelectMenuProps {

}

export const SelectMenu = component$<SelectMenuProps>((props) => {
  return (
    <div>
      SelectMenu component works!
    </div>
  );
});
