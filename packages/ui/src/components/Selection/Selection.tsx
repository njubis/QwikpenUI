import { component$ } from '@builder.io/qwik';

export interface SelectionProps {

}

export const Selection = component$<SelectionProps>((props) => {
  return (
    <div>
      Selection component works!
    </div>
  );
});
