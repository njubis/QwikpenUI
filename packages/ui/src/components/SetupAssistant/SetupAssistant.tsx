import { component$ } from '@builder.io/qwik';

export interface SetupAssistantProps {

}

export const SetupAssistant = component$<SetupAssistantProps>((props) => {
  return (
    <div>
      SetupAssistant component works!
    </div>
  );
});
