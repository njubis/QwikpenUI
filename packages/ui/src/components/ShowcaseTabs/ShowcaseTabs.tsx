import { component$ } from '@builder.io/qwik';

export interface ShowcaseTabsProps {

}

export const ShowcaseTabs = component$<ShowcaseTabsProps>((props) => {
  return (
    <div>
      ShowcaseTabs component works!
    </div>
  );
});
