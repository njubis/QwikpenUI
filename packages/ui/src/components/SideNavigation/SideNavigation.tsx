import { component$ } from '@builder.io/qwik';

export interface SideNavigationProps {

}

export const SideNavigation = component$<SideNavigationProps>((props) => {
  return (
    <div>
      SideNavigation component works!
    </div>
  );
});
