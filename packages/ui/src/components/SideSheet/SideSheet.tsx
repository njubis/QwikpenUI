import { component$ } from '@builder.io/qwik';

export interface SideSheetProps {

}

export const SideSheet = component$<SideSheetProps>((props) => {
  return (
    <div>
      SideSheet component works!
    </div>
  );
});
