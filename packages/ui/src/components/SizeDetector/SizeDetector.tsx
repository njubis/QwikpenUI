import { component$ } from '@builder.io/qwik';

export interface SizeDetectorProps {

}

export const SizeDetector = component$<SizeDetectorProps>((props) => {
  return (
    <div>
      SizeDetector component works!
    </div>
  );
});
