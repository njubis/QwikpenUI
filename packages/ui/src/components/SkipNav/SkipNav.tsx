import { component$ } from '@builder.io/qwik';

export interface SkipNavProps {

}

export const SkipNav = component$<SkipNavProps>((props) => {
  return (
    <div>
      SkipNav component works!
    </div>
  );
});
