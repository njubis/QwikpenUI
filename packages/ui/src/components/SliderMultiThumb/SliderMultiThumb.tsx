import { component$ } from '@builder.io/qwik';

export interface SliderMultiThumbProps {

}

export const SliderMultiThumb = component$<SliderMultiThumbProps>((props) => {
  return (
    <div>
      SliderMultiThumb component works!
    </div>
  );
});
