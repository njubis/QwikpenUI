import { component$ } from '@builder.io/qwik';

export interface SnackbarsProps {

}

export const Snackbars = component$<SnackbarsProps>((props) => {
  return (
    <div>
      Snackbars component works!
    </div>
  );
});
