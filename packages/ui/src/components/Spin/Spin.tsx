import { component$ } from '@builder.io/qwik';

export interface SpinProps {

}

export const Spin = component$<SpinProps>((props) => {
  return (
    <div>
      Spin component works!
    </div>
  );
});
