import { component$ } from '@builder.io/qwik';

export interface SpinButtonProps {

}

export const SpinButton = component$<SpinButtonProps>((props) => {
  return (
    <div>
      SpinButton component works!
    </div>
  );
});
