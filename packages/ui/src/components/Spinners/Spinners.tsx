import { component$ } from '@builder.io/qwik';

export interface SpinnersProps {

}

export const Spinners = component$<SpinnersProps>((props) => {
  return (
    <div>
      Spinners component works!
    </div>
  );
});
