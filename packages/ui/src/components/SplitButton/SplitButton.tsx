import { component$ } from '@builder.io/qwik';

export interface SplitButtonProps {

}

export const SplitButton = component$<SplitButtonProps>((props) => {
  return (
    <div>
      SplitButton component works!
    </div>
  );
});
