import { component$ } from '@builder.io/qwik';

export interface SplitViewProps {

}

export const SplitView = component$<SplitViewProps>((props) => {
  return (
    <div>
      SplitView component works!
    </div>
  );
});
