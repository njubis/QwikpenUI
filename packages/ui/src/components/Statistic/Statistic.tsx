import { component$ } from '@builder.io/qwik';

export interface StatisticProps {

}

export const Statistic = component$<StatisticProps>((props) => {
  return (
    <div>
      Statistic component works!
    </div>
  );
});
