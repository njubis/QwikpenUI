import { component$ } from '@builder.io/qwik';

export interface StatusLightProps {

}

export const StatusLight = component$<StatusLightProps>((props) => {
  return (
    <div>
      StatusLight component works!
    </div>
  );
});
