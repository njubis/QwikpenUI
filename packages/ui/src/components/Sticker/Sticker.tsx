import { component$ } from '@builder.io/qwik';

export interface StickerProps {

}

export const Sticker = component$<StickerProps>((props) => {
  return (
    <div>
      Sticker component works!
    </div>
  );
});
