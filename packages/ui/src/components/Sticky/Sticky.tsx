import { component$ } from '@builder.io/qwik';

export interface StickyProps {

}

export const Sticky = component$<StickyProps>((props) => {
  return (
    <div>
      Sticky component works!
    </div>
  );
});
