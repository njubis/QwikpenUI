import { component$ } from '@builder.io/qwik';

export interface StructuredListProps {

}

export const StructuredList = component$<StructuredListProps>((props) => {
  return (
    <div>
      StructuredList component works!
    </div>
  );
});
