import { component$ } from '@builder.io/qwik';

export interface SubmitProps {

}

export const Submit = component$<SubmitProps>((props) => {
  return (
    <div>
      Submit component works!
    </div>
  );
});
