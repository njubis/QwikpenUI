import { component$ } from '@builder.io/qwik';

export interface SwatchColorPickerProps {

}

export const SwatchColorPicker = component$<SwatchColorPickerProps>((props) => {
  return (
    <div>
      SwatchColorPicker component works!
    </div>
  );
});
