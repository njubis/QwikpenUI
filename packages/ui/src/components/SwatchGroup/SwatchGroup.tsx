import { component$ } from '@builder.io/qwik';

export interface SwatchGroupProps {

}

export const SwatchGroup = component$<SwatchGroupProps>((props) => {
  return (
    <div>
      SwatchGroup component works!
    </div>
  );
});
