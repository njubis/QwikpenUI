import { component$ } from '@builder.io/qwik';

export interface SwitchboardProps {

}

export const Switchboard = component$<SwitchboardProps>((props) => {
  return (
    <div>
      Switchboard component works!
    </div>
  );
});
