import { component$ } from '@builder.io/qwik';

export interface SymbolProps {

}

export const Symbol = component$<SymbolProps>((props) => {
  return (
    <div>
      Symbol component works!
    </div>
  );
});
