import { component$ } from '@builder.io/qwik';

export interface TabBarProps {

}

export const TabBar = component$<TabBarProps>((props) => {
  return (
    <div>
      TabBar component works!
    </div>
  );
});
