import { component$ } from '@builder.io/qwik';

export interface TabIndicatorProps {

}

export const TabIndicator = component$<TabIndicatorProps>((props) => {
  return (
    <div>
      TabIndicator component works!
    </div>
  );
});
