import { component$ } from '@builder.io/qwik';

export interface TabScrollerProps {

}

export const TabScroller = component$<TabScrollerProps>((props) => {
  return (
    <div>
      TabScroller component works!
    </div>
  );
});
