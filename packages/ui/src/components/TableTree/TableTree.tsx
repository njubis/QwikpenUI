import { component$ } from '@builder.io/qwik';

export interface TableTreeProps {

}

export const TableTree = component$<TableTreeProps>((props) => {
  return (
    <div>
      TableTree component works!
    </div>
  );
});
