import { component$ } from '@builder.io/qwik';

export interface TagGroupProps {

}

export const TagGroup = component$<TagGroupProps>((props) => {
  return (
    <div>
      TagGroup component works!
    </div>
  );
});
