import { component$ } from '@builder.io/qwik';

export interface TagInputProps {

}

export const TagInput = component$<TagInputProps>((props) => {
  return (
    <div>
      TagInput component works!
    </div>
  );
});
