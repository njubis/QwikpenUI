import { component$ } from '@builder.io/qwik';

export interface TelephoneProps {

}

export const Telephone = component$<TelephoneProps>((props) => {
  return (
    <div>
      Telephone component works!
    </div>
  );
});
