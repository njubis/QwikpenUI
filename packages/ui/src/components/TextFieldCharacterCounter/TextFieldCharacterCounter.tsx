import { component$ } from '@builder.io/qwik';

export interface TextFieldCharacterCounterProps {

}

export const TextFieldCharacterCounter = component$<TextFieldCharacterCounterProps>((props) => {
  return (
    <div>
      TextFieldCharacterCounter component works!
    </div>
  );
});
