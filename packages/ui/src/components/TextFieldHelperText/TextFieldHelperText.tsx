import { component$ } from '@builder.io/qwik';

export interface TextFieldHelperTextProps {

}

export const TextFieldHelperText = component$<TextFieldHelperTextProps>((props) => {
  return (
    <div>
      TextFieldHelperText component works!
    </div>
  );
});
