import { component$ } from '@builder.io/qwik';

export interface TextFieldIconProps {

}

export const TextFieldIcon = component$<TextFieldIconProps>((props) => {
  return (
    <div>
      TextFieldIcon component works!
    </div>
  );
});
