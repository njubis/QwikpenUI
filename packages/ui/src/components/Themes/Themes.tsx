import { component$ } from '@builder.io/qwik';

export interface ThemesProps {

}

export const Themes = component$<ThemesProps>((props) => {
  return (
    <div>
      Themes component works!
    </div>
  );
});
