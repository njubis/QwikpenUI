import { component$ } from '@builder.io/qwik';

export interface TileProps {

}

export const Tile = component$<TileProps>((props) => {
  return (
    <div>
      Tile component works!
    </div>
  );
});
