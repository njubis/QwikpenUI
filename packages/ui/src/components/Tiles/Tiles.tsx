import { component$ } from '@builder.io/qwik';

export interface TilesProps {

}

export const Tiles = component$<TilesProps>((props) => {
  return (
    <div>
      Tiles component works!
    </div>
  );
});
