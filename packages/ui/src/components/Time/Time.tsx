import { component$ } from '@builder.io/qwik';

export interface TimeProps {

}

export const Time = component$<TimeProps>((props) => {
  return (
    <div>
      Time component works!
    </div>
  );
});
