import { component$ } from '@builder.io/qwik';

export interface TimePickerProps {

}

export const TimePicker = component$<TimePickerProps>((props) => {
  return (
    <div>
      TimePicker component works!
    </div>
  );
});
