import { component$ } from '@builder.io/qwik';

export interface TitleBarsProps {

}

export const TitleBars = component$<TitleBarsProps>((props) => {
  return (
    <div>
      TitleBars component works!
    </div>
  );
});
