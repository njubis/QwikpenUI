import { component$ } from '@builder.io/qwik';

export interface ToastContainerProps {

}

export const ToastContainer = component$<ToastContainerProps>((props) => {
  return (
    <div>
      ToastContainer component works!
    </div>
  );
});
