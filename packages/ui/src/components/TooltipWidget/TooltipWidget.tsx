import { component$ } from '@builder.io/qwik';

export interface TooltipWidgetProps {

}

export const TooltipWidget = component$<TooltipWidgetProps>((props) => {
  return (
    <div>
      TooltipWidget component works!
    </div>
  );
});
