import { component$ } from '@builder.io/qwik';

export interface TooltipsProps {

}

export const Tooltips = component$<TooltipsProps>((props) => {
  return (
    <div>
      Tooltips component works!
    </div>
  );
});
