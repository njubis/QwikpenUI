import { component$ } from '@builder.io/qwik';

export interface TopAppBarProps {

}

export const TopAppBar = component$<TopAppBarProps>((props) => {
  return (
    <div>
      TopAppBar component works!
    </div>
  );
});
