import { component$ } from '@builder.io/qwik';

export interface TransferProps {

}

export const Transfer = component$<TransferProps>((props) => {
  return (
    <div>
      Transfer component works!
    </div>
  );
});
