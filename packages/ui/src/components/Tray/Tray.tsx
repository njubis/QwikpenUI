import { component$ } from '@builder.io/qwik';

export interface TrayProps {

}

export const Tray = component$<TrayProps>((props) => {
  return (
    <div>
      Tray component works!
    </div>
  );
});
