import { component$ } from '@builder.io/qwik';

export interface TreeSelectProps {

}

export const TreeSelect = component$<TreeSelectProps>((props) => {
  return (
    <div>
      TreeSelect component works!
    </div>
  );
});
