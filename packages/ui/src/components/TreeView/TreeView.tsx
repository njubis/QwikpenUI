import { component$ } from '@builder.io/qwik';

export interface TreeViewProps {

}

export const TreeView = component$<TreeViewProps>((props) => {
  return (
    <div>
      TreeView component works!
    </div>
  );
});
