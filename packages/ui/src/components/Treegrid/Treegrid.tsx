import { component$ } from '@builder.io/qwik';

export interface TreegridProps {

}

export const Treegrid = component$<TreegridProps>((props) => {
  return (
    <div>
      Treegrid component works!
    </div>
  );
});
