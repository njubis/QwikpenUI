import { component$ } from '@builder.io/qwik';

export interface TrialBarProps {

}

export const TrialBar = component$<TrialBarProps>((props) => {
  return (
    <div>
      TrialBar component works!
    </div>
  );
});
