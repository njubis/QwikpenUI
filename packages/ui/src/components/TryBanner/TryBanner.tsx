import { component$ } from '@builder.io/qwik';

export interface TryBannerProps {

}

export const TryBanner = component$<TryBannerProps>((props) => {
  return (
    <div>
      TryBanner component works!
    </div>
  );
});
