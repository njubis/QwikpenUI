import { component$ } from '@builder.io/qwik';

export interface TypographyProps {

}

export const Typography = component$<TypographyProps>((props) => {
  return (
    <div>
      Typography component works!
    </div>
  );
});
