import { component$ } from '@builder.io/qwik';

export interface UIShellProps {

}

export const UIShell = component$<UIShellProps>((props) => {
  return (
    <div>
      UIShell component works!
    </div>
  );
});
