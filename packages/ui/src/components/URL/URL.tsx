import { component$ } from '@builder.io/qwik';

export interface URLProps {

}

export const URL = component$<URLProps>((props) => {
  return (
    <div>
      URL component works!
    </div>
  );
});
