import { component$ } from '@builder.io/qwik';

export interface VerticalNavigationProps {

}

export const VerticalNavigation = component$<VerticalNavigationProps>((props) => {
  return (
    <div>
      VerticalNavigation component works!
    </div>
  );
});
