import { component$ } from '@builder.io/qwik';

export interface VerticalTabsProps {

}

export const VerticalTabs = component$<VerticalTabsProps>((props) => {
  return (
    <div>
      VerticalTabs component works!
    </div>
  );
});
