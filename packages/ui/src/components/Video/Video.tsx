import { component$ } from '@builder.io/qwik';

export interface VideoProps {

}

export const Video = component$<VideoProps>((props) => {
  return (
    <div>
      Video component works!
    </div>
  );
});
