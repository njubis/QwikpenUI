import { component$ } from '@builder.io/qwik';

export interface VisibilityProps {

}

export const Visibility = component$<VisibilityProps>((props) => {
  return (
    <div>
      Visibility component works!
    </div>
  );
});
